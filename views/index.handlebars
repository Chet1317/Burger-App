           <style>
           .col-sm-3 {
    -webkit-box-flex: 0;
    -ms-flex: 0 0 25%;
    flex: 0 0 25%;
    max-width: 25%;
    position: absolute;
   
}
.input-group-text {
    color: #000000;
    border-color: red;
    font-size: 1.5rem;
    background-color: #d4e23cc2;
}
</style>

            

<div class="container">
     <div class="row justify-content-center">
        <div class="col-xs">
                {{#each availBurger}}
   
            <span class="input-group-text">{{burger_name}}</span>
            <a style="float: right;" href="/devoured/{{id}}" type="button" class="btn btn-danger">Devour<a>
           
                    {{/each}}
               </div>
                  {{#each burgDev}}
                <div class="col-xs">
        <div  class="alert alert-warning container" role="alert">Devoured it!
                   </div>
         </div>
        {{/each}}
  


      
     
     </div>
 
</div>
