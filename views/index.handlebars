{{!-- <style>
        a:not([href]):not([tabindex]) {
    color: inherit;
    text-decoration: none;
    text-align-last: right;
    margin-left: 500px;
}
.row {
    display: -webkit-box;
    display: -ms-flexbox;
    display: flex;
    -ms-flex-wrap: wrap;
    flex-wrap: wrap;
    margin-right: 200px;
    margin-left: 200px;
}
</style> --}}


<div class="container">
     <div class="row">
        <div class="col-sm-3">
{{#each availBurger}}
   
            <span class="input-group-text">{{burger_name}}</span>
            <a style="float: right;" href="/devoured/{{id}}" type="button" class="btn btn-danger">Devour<a>
                    {{/each}}
      </div>

        {{#each burgDev}}

        <div  class="alert alert-warning container" role="alert">Devoured it!</div>
        {{/each}}
       
     </div>
 </div>
